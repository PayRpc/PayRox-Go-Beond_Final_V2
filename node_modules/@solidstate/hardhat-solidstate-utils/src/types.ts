import type { PackageJson } from '@nomicfoundation/hardhat-utils/package';

type Matcher = Parameters<typeof String.prototype.match>[0];

export type FilterOptions = {
  only?: Matcher[];
  except?: Matcher[];
};

export type PackageData = {
  name: string;
  latestVersion?: string;
  installedVersion?: string;
  loaded?: boolean;
};

export interface HardhatPackageJson extends PackageJson {
  bin: { hardhat: string };
}
