import { TASK_SOLIDSTATE_PLUGINS } from '../task_names.js';
import { task } from 'hardhat/config';

export default task(TASK_SOLIDSTATE_PLUGINS)
  .setDescription('List available and installed plugins from Solidstate')
  .setAction(() => import('../actions/solidstate_plugins.js'))
  .build();
