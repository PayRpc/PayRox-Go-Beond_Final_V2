import { TASK_SOLIDSTATE_FUNDING } from '../task_names.js';
import { task } from 'hardhat/config';
export default task(TASK_SOLIDSTATE_FUNDING)
    .setDescription('Help fund open-source software from Solidstate')
    .setAction(() => import('../actions/solidstate_funding.js'))
    .build();
